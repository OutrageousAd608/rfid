#ifndef TOUCH_H
#define TOUCH_H

#include "main.h"

// Returns 1 if screen is currently pressed, 0 if not
uint8_t Touch_IsPressed(void);

// Reads the raw coordinates (0-4095)
// Returns 1 if successful, 0 if touch was unstable
uint8_t Touch_GetXY(uint16_t *x, uint16_t *y);

#endif
